Paint Mountain UI refinement — ImageGen prompt set, revision 03
Date: 2026-08-20
Mode: built-in ImageGen edit/compositing; preserve the matching running-game world and replace only the UI.

Shared direction
- Compact Overlay: clear, calm, icon-first, world-first, and shared-component-only.
- No panels, cards, boxes, sheets, glass, top bars, full-width bottom strips, paragraphs, or decorative section containers.
- One soft edge contrast scrim is allowed where direct text/icons need contrast.
- Use the same 20/24 px vector-like icon grid, metric, ScoreScale, BallQueue, ValueStepper, ActionControl, and state grammar.
- One filled blue primary action. Red and green are paint semantics paired with different token shapes.
- The complete score domain is always 0–100. Live Aim/Map/Shot Follow use the vertical preset with labels 100, 75, 50, 25, 0; Briefing/Result keep the horizontal preset.
- Preserve mountain, cannon, camera, lighting, terrain, paint, sky, and game identity. Add no world objects or fake mechanics.

01A — Aiming / Edge Balance
- Actual Aim screenshot is the edit target; revision-02 Aim image is style reference only.
- Vertical ScoreScale at far left; StageIdentity upper-left; metrics upper-right.
- Three shape-coded balls stacked at far right with empty space for hover/focus tooltips.
- Angle, power, and Fire form one compact lower-center sequence.

01B — Aiming / Right Instruments
- Preserve the same world and shared component styling.
- Keep the left edge mostly clear.
- Put the vertical ScoreScale at far right and the vertical BallQueue immediately beside it.
- Keep angle, power, and Fire in one compact lower sequence.

01C — Aiming / Cannon Focus
- Vertical ScoreScale at far left.
- Put the BallQueue in one horizontal row below upper-right metrics.
- Split the lower controls around the cannon: angle on the left, Fire under the cannon, power on the right.

02 — Stage Select / Actual terrain preview
- Current Stage Select supplies data truth; revision-02 Stage Select supplies the StageRail layout; current Stage 01 Aim supplies actual terrain geometry/render.
- Preserve the full-bleed StageRail layout but replace the generic alpine illustration with the actual selected StageRuntimeArtifact terrain.
- Show StageIdentity, direct target facts, eight stage nodes, Start, and Back only.

03 — Briefing
- Direct StageIdentity, target icon/value, horizontal complete ScoreScale, and shared BallQueue.
- At most two ContextHints and direct Back/Start actions.

04 — Result
- The painted mountain is the result hero.
- Direct large score/verdict, horizontal complete ScoreScale, compact R/G metrics, and at most three actions.

Note
Generated copy, values, icon drawings, and world pixels are composition references, not runtime evidence. The HTML report adds real keyboard-accessible variant selection and BallQueue hover/focus/press explanations over the static mockups.
